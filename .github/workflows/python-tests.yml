name: Python Tests

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  clients-python:
    name: clients/python (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - windows-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install package and test dependencies
        working-directory: clients/python
        run: python -m pip install --upgrade pip setuptools wheel pytest -e .

      - name: Compile Python sources
        run: python -m compileall clients/python/src

      - name: Run non-device tests
        run: python -m pytest clients/python/tests -m "not device"
